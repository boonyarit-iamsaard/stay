# Local runs of .github/workflows with `act`.
# The default runner image has no node, git, or docker CLI; this one does.
-P ubuntu-latest=catthehacker/ubuntu:act-latest
--container-architecture linux/amd64
